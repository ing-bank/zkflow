The files in this folder are for debug purposes, so they area not included in the compiled circuit.

validate_group_hashes.zn checks if computed component group hashes match the group hashes received 
in witness. 