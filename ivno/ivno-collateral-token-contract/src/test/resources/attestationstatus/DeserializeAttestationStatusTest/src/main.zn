mod attestation_status;

use attestation_status::AttestationStatus;
use attestation_status::SerializedAttestationStatus;

fn main(witness: SerializedAttestationStatus) -> AttestationStatus {
    AttestationStatus::deserialize(witness)
}