mod automatic_hash_constraint;
mod poly_automatic_hash_constraint;

use automatic_hash_constraint::AutomaticHashConstraint;
use poly_automatic_hash_constraint::PolyAutomaticHashConstraint;
use poly_automatic_hash_constraint::SerializedPolyAutomaticHashConstraint;

fn main(witness: SerializedPolyAutomaticHashConstraint) -> AutomaticHashConstraint {
    PolyAutomaticHashConstraint::deserialize(witness).value
}