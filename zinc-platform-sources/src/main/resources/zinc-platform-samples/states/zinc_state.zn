const ZINC_STATE_LENGTH: u16 = 0;

type SerializedZincState = [u8; ZINC_STATE_LENGTH];

struct ZincState;

impl ZincState {
    fn deserialize(serialized: SerializedZincState) -> ZincState {
        ZincState
    }

    fn equals(this: ZincState, that: ZincState) -> bool {
        true
    }
}